'use client';

import * as React from "react";

// Simplified calendar placeholder - not used in this project
function Calendar({ className }: { className?: string }) {
  return <div className={className}>Calendar</div>;
}
Calendar.displayName = "Calendar";

export { Calendar };
